# Core deep learning stack (pin to PyTorch 2.8.0)
torch==2.8.0
# TorchVision version aligned with torch 2.8.x
torchvision==0.23.0
# Optional but recommended if audio utilities are needed
torchaudio==2.8.0

# Numerics & arrays
numpy>=1.25

# Image IO for datasets
pillow>=9.5

# Progress and experiment tracking
tqdm>=4.66
wandb>=0.17.0

# Analysis / visualization (used in analyze/ and diagnostics)
scikit-learn>=1.3.0
matplotlib>=3.7.0
