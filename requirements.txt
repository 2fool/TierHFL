# Core deep learning stack (pin to PyTorch 2.7.9 to resolve conflicts)
torch==2.7.9
torchvision==0.22.0
torchaudio==2.7.9

# Numerics & arrays (pin to compatible versions)
numpy==2.0.2

# Image IO for datasets
pillow>=9.5

# Progress and experiment tracking
tqdm>=4.66
wandb>=0.17.0

# Analysis / visualization (pin to compatible versions)
scikit-learn==1.5.2
matplotlib>=3.7.0
